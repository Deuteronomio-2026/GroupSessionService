server.port=8084
spring.application.name=group-session-service

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/groupsessiondb}
spring.datasource.username=${DB_USER:jacobo}
spring.datasource.password=${DB_PASSWORD:jacobo123}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

management.endpoints.web.exposure.include=health,info

# RabbitMQ
spring.rabbitmq.addresses=${RABBITMQ_URL:amqps://znyracho:6OWkLPKTK6ayX4_Kc7ayjS2KeVROhj0d@moose.rmq.cloudamqp.com/znyracho}

# Exchange y routing keys
rabbitmq.exchange=mindbridge.exchange
rabbitmq.routing-key.session-requested=session.requested
rabbitmq.routing-key.session-approved=session.approved
rabbitmq.routing-key.session-enrolled=session.enrolled
rabbitmq.routing-key.session-cancelled=session.cancelled